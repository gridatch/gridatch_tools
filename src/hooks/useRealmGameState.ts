import { WallTile, HandState } from "../types/simulation";

/**
 * 牌山から手牌に引く処理を行うフック
 * @param wall 牌山
 * @param handState 手牌
 */
export const useGameState = (
  wall: WallTile[],
  handState: HandState,
) => {

};